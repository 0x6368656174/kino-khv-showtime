import QtQuick 2.0

Item {
    height: 4 * sc

    Rectangle {
        width: parent.width
        height: 2 * sc
        color: "#06b0ef"
    }
    Rectangle {
        width: parent.width
        height: 2 * sc
        y: 2 * sc
        color: "#0082b2"
    }
}
